<x-filament-panels::page>
    <div class="fi-section-content-ctn">
        <p class="text-sm text-gray-600 dark:text-gray-400">
            Move this whole app between environments. <strong>Download export</strong> bundles every
            collection, state, function, flow and page (plus the home / 404 / maintenance page settings)
            into one JSON document. <strong>Import</strong> replays that document — collections, pages and
            flows are matched by key/slug and updated in place, so re-importing is safe and repeatable.
        </p>
    </div>
</x-filament-panels::page>
